# Base URL of the ConstructAI backend API.
# Local dev defaults to http://localhost:8000 if this is unset.
# On Vercel, set this to your Render backend URL, e.g.:
#   VITE_API_URL=https://constructai-api.onrender.com
VITE_API_URL=http://localhost:8000
