"""Domain services — the procurement 'brain' and the weather provider.

Kept separate from the routers so the logic stays pure and testable, and so the
future AI/agent layer can call these directly.
"""
